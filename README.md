# CBC Weather App by Abby Christens

## Steps to Install

Git clone the project to your desktop via the command line
From the command line, `cd` into the project
Run `npm install`
Run `npm start`
Party!
